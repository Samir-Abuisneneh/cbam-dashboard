streamlit==1.60.0
pandas==3.0.5
plotly==6.9.0
# Pinned: streamlit 1.60.0 wraps a private Starlette class
# (_MediaAwareGZipResponder) whose constructor changed in starlette 1.4.0
# to require a new keyword-only arg, crashing every request with
# "TypeError: GZipResponder.__init__() missing 1 required keyword-only
# argument: 'thread_minimum_size'". 1.3.1 is the last version confirmed
# working with this streamlit release. Unpin only after confirming a newer
# streamlit release supports the newer starlette.
starlette==1.3.1
