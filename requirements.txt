streamlit==1.60.0
pandas==3.0.5
plotly==6.9.0
# Pinned: streamlit 1.60.0 wraps a private Starlette class
# (_MediaAwareGZipResponder) whose constructor changed in starlette 1.4.0
# to require a new keyword-only arg, crashing every request with
# "TypeError: GZipResponder.__init__() missing 1 required keyword-only
# argument: 'thread_minimum_size'". 1.3.1 is the last version confirmed
# working with this streamlit release. Unpin only after confirming a newer
# streamlit release supports the newer starlette.
starlette==1.3.1

# Solver for the sourcing allocation LP (cbam_model/optimisation).
# HiGHS via scipy.optimize.linprog; the formulation ports to GLPK unchanged.
scipy==1.16.3

# Required by pandas Styler.background_gradient, used to colour the forecast
# skill grid on the dashboard.
matplotlib==3.11.1

# GradientBoostingRegressor for the one machine-learning model in the price
# forecast (cbam_model/forecasting/price_model.py:gradient_boosted), run
# through the same walk-forward harness as the statistical models on the
# "Price forecast" dashboard tab. Not optional there, so it belongs in the
# deployment set rather than requirements-dev.txt.
scikit-learn==1.9.0
